name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Extract version from tag
        id: version
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}" -replace '^v', ''
          "VERSION=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Set version in project
        shell: pwsh
        run: |
          $file = "src/PRDock.App/PRDock.App.csproj"
          (Get-Content $file) -replace '<Version>.*</Version>', "<Version>${{ steps.version.outputs.VERSION }}</Version>" | Set-Content $file

      - name: Publish
        run: >
          dotnet publish src/PRDock.App
          -c Release
          -r win-x64
          --self-contained
          -o publish

      - name: Install Velopack CLI
        run: dotnet tool install -g vpk

      - name: Download previous release (for delta generation)
        run: vpk download github --repoUrl https://github.com/${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Pack with Velopack
        run: >
          vpk pack
          --packId PRDock
          --packVersion ${{ steps.version.outputs.VERSION }}
          --packDir publish
          --mainExe PRDock.App.exe

      - name: Upload to GitHub Releases
        run: >
          vpk upload github
          --repoUrl https://github.com/${{ github.repository }}
          --token ${{ secrets.GITHUB_TOKEN }}
          --tag ${{ github.ref_name }}
