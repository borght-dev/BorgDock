// TODO: Compose all layout and feature components here after all units are merged.
// This minimal shell verifies the scaffold builds and renders.
export default function App() {
  return (
    <div className="h-screen w-screen bg-[var(--color-background)] text-[var(--color-text-primary)] flex items-center justify-center">
      <p className="text-sm text-[var(--color-text-secondary)]">PRDock — Tauri</p>
    </div>
  );
}
