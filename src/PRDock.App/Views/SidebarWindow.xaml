<Window x:Class="PRDock.App.Views.SidebarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PRDock.App.ViewModels"
        Title="PRDock"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="420"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header row -->
        <Grid Grid.Row="0" Margin="12,10,12,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="PRDock"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextPrimaryBrush}" />

            <ToggleButton Grid.Column="1"
                          Content="Pin"
                          IsChecked="{Binding IsPinned}"
                          Command="{Binding TogglePinCommand}"
                          Margin="4,0"
                          VerticalAlignment="Center"
                          ToolTip="Pin / Unpin sidebar" />

            <Button Grid.Column="2"
                    Content="_"
                    Command="{Binding MinimizeToBadgeCommand}"
                    Width="28" Height="28"
                    Margin="4,0"
                    VerticalAlignment="Center"
                    ToolTip="Minimize to badge" />

            <Button Grid.Column="3"
                    Content="..."
                    Command="{Binding OpenSettingsCommand}"
                    Width="28" Height="28"
                    Margin="4,0"
                    VerticalAlignment="Center"
                    ToolTip="Settings" />
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                Padding="12,6"
                Margin="12,0,12,6"
                CornerRadius="4">
            <TextBlock Text="{Binding StatusText}"
                       FontSize="12"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
        </Border>

        <!-- Main content area (placeholder) -->
        <Grid Grid.Row="2" Margin="12,0,12,12">
            <TextBlock Text="No PRs loaded"
                       FontSize="14"
                       ExampletalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
        </Grid>
    </Grid>
</Window>
